{
    "extends": ["config:base"],
    "schedule": ["every weekend"],
    "packageRules": [
        {
            "matchDepTypes": ["dependencies", "devDependencies"],
            "automerge": true
        }
    ]
}
